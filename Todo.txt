Important todos:

2. clean termination with proper return codes (no exits!!). Which errors should lead to aborts and which can be skipped? How to detect if (on open) a disk is already open? 
3. kill singletons?

